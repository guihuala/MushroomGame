using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TransportIItemPort.cspublicinterfaceIItemPort{往外给货（从该端口输出）boolTryPull(refItemPayloadpayload);往里收货（向该端口输入）boolTryPush(inItemPayloadpayload);是否还有可输出可接收boolCanPull{get;}boolCanPush{get;}} : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
